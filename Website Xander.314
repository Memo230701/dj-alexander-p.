<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DJ Alexander P.</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap');
        
        body {
            background-color: #000;
            color: #0ff;
            font-family: 'Orbitron', sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            overflow-y: auto;
            position: relative;
        }
        
        .lasers {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
            pointer-events: none;
        }

        .laser {
            position: absolute;
            width: 2px;
            height: 100vh;
            background: cyan;
            opacity: 0.7;
            animation: laserAnim 3s linear infinite;
        }

        @keyframes laserAnim {
            from {
                transform: translateY(-100%);
            }
            to {
                transform: translateY(100%);
            }
        }
        
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            position: relative;
            z-index: 2;
            padding: 20px;
        }
        
        h1 {
            font-size: 3rem;
            text-shadow: 0 0 20px #0ff, 0 0 40px #00f;
            animation: pulse 1.5s infinite alternate;
        }
        
        @keyframes pulse {
            from {
                text-shadow: 0 0 20px #0ff, 0 0 40px #00f;
            }
            to {
                text-shadow: 0 0 30px #0ff, 0 0 60px #00f;
            }
        }
        
        .bio {
            max-width: 600px;
            font-size: 1.2rem;
            line-height: 1.5;
            background: rgba(0, 0, 0, 0.7);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px #0ff;
            margin-bottom: 20px;
            position: relative;
            z-index: 3;
        }
        
        .links a {
            display: block;
            margin: 15px;
            padding: 15px 30px;
            text-decoration: none;
            color: #000;
            background: linear-gradient(90deg, #ff00ff, #00ffff);
            border-radius: 10px;
            font-size: 1.5rem;
            font-weight: bold;
            text-transform: uppercase;
            transition: 0.3s;
            box-shadow: 0 0 10px #fff;
        }
        
        .links a:hover {
            background: linear-gradient(90deg, #00ffff, #ff00ff);
            box-shadow: 0 0 20px #fff;
        }
        
        .contact-form {
            background: rgba(0, 0, 0, 0.8);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px #0ff;
            max-width: 400px;
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            z-index: 3;
        }

        .contact-form input, .contact-form textarea {
            width: calc(100% - 20px);
            padding: 10px;
            margin-top: 10px;
            background: #222;
            border: none;
            color: #0ff;
            font-size: 1rem;
            border-radius: 5px;
            text-align: center;
        }

        .contact-form button {
            margin-top: 10px;
            padding: 10px;
            background: linear-gradient(90deg, #ff00ff, #00ffff);
            border: none;
            color: #000;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 5px;
            transition: 0.3s;
            box-shadow: 0 0 10px #fff;
        }

        .contact-form button:hover {
            background: linear-gradient(90deg, #00ffff, #ff00ff);
            box-shadow: 0 0 20px #fff;
        }
    </style>
</head>
<body>
    <div class="lasers"></div>
    <div class="container">
        <h1>DJ Alexander P.</h1>
        <div class="bio">
            <p>Alexander P., bekannt als <strong>Xander.314</strong>, ist ein 20-jähriger DJ, der seit 6 Jahren aktiv ist. Seine Leidenschaft für Hardtechno und Trance brachte ihn bereits auf zahlreiche Events. Neben seiner Karriere als DJ studiert er zurzeit an der Hochschule Düsseldorf.</p>
        </div>
        <div class="links">
            <a href="https://www.instagram.com/xander.314/" target="_blank">Instagram</a>
            <a href="https://www.youtube.com/@xander.314" target="_blank">YouTube</a>
            <a href="https://soundcloud.com/xander314" target="_blank">SoundCloud</a>
        </div>
        <div class="contact-form">
            <h2>Kontakt</h2>
            <form action="https://formspree.io/f/mnnpnvpw" method="POST">
                <input type="text" name="name" placeholder="Dein Name" required>
                <input type="email" name="email" placeholder="Deine E-Mail" required>
                <input type="tel" name="phone" placeholder="Deine Telefonnummer" required>
                <textarea name="message" rows="4" placeholder="Deine Nachricht" required></textarea>
                <button type="submit">Senden</button>
            </form>
        </div>
    </div>
    <script>
        function createLasers() {
            const laserContainer = document.querySelector('.lasers');
            for (let i = 0; i < 10; i++) {
                const laser = document.createElement('div');
                laser.classList.add('laser');
                laser.style.left = Math.random() * 100 + 'vw';
                laser.style.animationDuration = (2 + Math.random() * 2) + 's';
                laserContainer.appendChild(laser);
            }
        }
        createLasers();
    </script>
</body>
</html>
